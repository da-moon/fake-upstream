
from .. import __version__


SWAGGER_CONFIG = {
    'title': 'simple upstream backend service',
    'description':
        'Webserver used as an upstream backend to help with observing how reverse proxy module affects requests . Use this service at your own risk.',
    'version': __version__,
    'hide_top_bar': True,
    'uiversion': 3
}
